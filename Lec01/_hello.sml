(* This is comments. (* You can have nested comments like this *) *)

val x = 42;

val y = 43;

val z = (x + y) + x * y;

val q = z+1;

val abs_of_z = if z > 0 then z else 0 - z;

val abs_of_z_simpler = abs z;
